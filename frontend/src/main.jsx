import React from "react";
import ReactDOM from "react-dom/client";
import CampusJustoCRM from "./CampusJustoCRM.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CampusJustoCRM />
  </React.StrictMode>
);
